{% extends 'base.html.twig' %}

{% block body %}
    <h1>Criar Atendimento</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Criar" class="btn btn-primary float-left"  />
    {{ form_end(form) }}

    
            <a href="{{ path('atendimento_index') }}" class="btn btn-secondary">Voltar</a>
    
{% endblock %}
