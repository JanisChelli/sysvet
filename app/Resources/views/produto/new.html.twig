{% extends 'base.html.twig' %}

{% block body %}
    <h1>Novo Produto</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Criar novo" class="btn btn-primary" />
    {{ form_end(form) }}

    
            <a href="{{ path('produto_index') }}" class="btn btn-dark float-right">Voltar a lista</a>
     
{% endblock %}
